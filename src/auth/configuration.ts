import { betterAuth } from "better-auth";import { drizzleAdapter } from "better-auth/adapters/drizzle";import { nextCookies } from "better-auth/next-js";import type { KeepUpDatabase } from "../database";import * as schema from "../database/schema";
export function createBetterAuth(database:KeepUpDatabase,options:{secret:string;baseURL?:string;allowPublicSignUp:boolean}){return betterAuth({database:drizzleAdapter(database,{provider:"pg",schema,transaction:true}),secret:options.secret,baseURL:options.baseURL,emailAndPassword:{enabled:true,disableSignUp:!options.allowPublicSignUp,requireEmailVerification:false,minPasswordLength:8,maxPasswordLength:128},plugins:[nextCookies()]});}
